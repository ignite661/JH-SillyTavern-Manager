v1.0.3
更新日志：
- 修复：允许 pnpm 运行依赖构建脚本，解决 ERR_PNPM_IGNORED_BUILDS 导致酒馆启动失败
- 加固：Node 版本检查改为软检查，低于建议版本不再阻断安装
- 加固：自动匹配酒馆项目要求的 pnpm 版本，避免 pnpm 版本不兼容
- 加固：克隆/下载/依赖安装失败时自动重试，减少网络波动导致的中断
- 加固：更新/重装依赖前检查酒馆是否在运行，避免操作冲突
- 加固：下载脚本后自动做语法校验，避免坏文件覆盖好文件
- 加固：酒馆更新改用 git pull --rebase --autostash，减少本地改动导致更新失败
- 修复：pkg 命令增加 --force-confdef + --force-confold，防止配置冲突中断升级
- 修复：安装前先执行 pkg upgrade，避免 curl/openssl 版本不同步导致链接错误
- 全新轻量化重构，原生 Termux 直跑
- 启动零网络请求，秒开不卡顿
- 支持备份 / 恢复酒馆数据
- 支持一键检查酒馆更新
- 支持一键更新脚本自身
