v1.0.9
更新日志：
- 修复：自动锁定 webpack 5.98.0，解决 Termux/Node 24 下 webpack 5.109.2 编译报错导致酒馆无法启动
- 安装/更新/重装依赖后会自动检测并锁定 webpack 稳定版
- 更新机制升级为 GitHub Release 整包更新，以后新增文件也能一次同步
- 新增：安装依赖增加 unzip，支持整包更新解压
- 修复：默认安装 nodejs-lts，避免 nodejs current 版本导致酒馆前端 webpack 编译失败
- 修复：pnpm 构建许可写入 pnpm-workspace.yaml，并给 pnpm install 增加 --dangerously-allow-all-builds
- 修复：更新脚本增加缓存绕过参数，避免 GitHub raw CDN 返回旧版 VERSION 导致误判“已是最新”
- 修复：版本相同时支持强制重新下载覆盖，方便手动刷新
- 加固：自动匹配酒馆项目要求的 pnpm 版本，避免 pnpm 版本不兼容
- 加固：克隆/下载/依赖安装失败时自动重试，减少网络波动导致的中断
- 加固：更新/重装依赖前检查酒馆是否在运行，避免操作冲突
- 加固：下载脚本后自动做语法校验，避免坏文件覆盖好文件
- 加固：酒馆更新改用 git pull --rebase --autostash，减少本地改动导致更新失败
- 修复：pkg 命令增加 --force-confdef + --force-confold，防止配置冲突中断升级
- 修复：安装前先执行 pkg upgrade，避免 curl/openssl 版本不同步导致链接错误
- 全新轻量化重构，原生 Termux 直跑
- 启动零网络请求，秒开不卡顿
- 支持备份 / 恢复酒馆数据
- 支持一键检查酒馆更新
- 支持一键更新脚本自身
